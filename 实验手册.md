# AI Coding Workshop · 实验手册

> **1 小时实操实验**——从环境就绪到 3 个 Task 全跑完
> 配套培训课件：http://120.79.157.186/
> 起步项目：本仓库 `workshop` 分支
> 培训方法论：`/think` → `/spec` → `/small-diff` → `/check` → `/summary` → `/update-context` / `/record-gotcha`

---

## 整体节奏

| 时段 | 内容 | 产出 |
|---|---|---|
| 00-05 | 准备：clone / install / dev 起来 | 后台能登录、看到 3 份 seed 答卷 |
| 05-15 | 项目熟悉：用 AI 自动生成 `wiki/` 文档 | `wiki/` 目录里 4-6 份 .md |
| 15-30 | **Task 1** · 答卷详情页 | `app/admin/exam/[id]/page.tsx` + spec |
| 30-45 | **Task 2** · 重评失败题 | API route + dashboard 按钮接通 |
| 45-55 | **Task 3** · 题型正确率统计 | `app/admin/stats/page.tsx` |
| 55-60 | 沉淀 + 分享：`/record-gotcha` + `/summary` | 一条 gotcha + 一句话感悟 |

每个 Task 走相同节奏：**`/think` 澄清 → 开 spec → 实施 → `/check` → `/summary` 收尾**。

---

# 阶段 1 · 准备（5 分钟）

## 1.1 拉代码

```bash
git clone -b workshop git@github.com:jackwener/exam-system.git
cd exam-system
pnpm install        # 顺带激活 .githooks/（pre-commit hook 自动启用）
pnpm dev            # http://localhost:3001
```

## 1.2 验证环境

打开浏览器：

| URL | 应看到 |
|---|---|
| http://localhost:3001/exam | 输入姓名开考的入口 |
| http://localhost:3001/admin/login | 管理员登录页 |

进 `/admin/login` 输入 **`workshop2026`** → 进入 dashboard，应该已经有 3 份 seed 答卷：

- 张高分 92 分（演示高分场景）
- 李及格 68 分（演示及格边界）
- 王挂科 42 分（演示缺答 + 不及格）

✅ 看到这 3 行 = 环境就绪。

> ⚠️ 不要配 `ANTHROPIC_API_KEY` —— 没配会走 mock grader（按答案长度阶梯给分），workshop 用 mock 就够。

---

# 阶段 2 · 项目熟悉 · 生成 Wiki（10 分钟）

让 Coding Agent 把整个项目扫一遍，给你产出一份**可读、分层、有内部链接**的 `wiki/` 目录——这是后续 Task 的资料库。

## 提示词

```md
我是一名研发工程师，计划基于当前的 exam-system 项目开发新功能。请帮我系统化梳理这个项目，输出一份让接手者能在 10 分钟内建立心智模型的 wiki。

请按下面要求做：

1. **扫描整个项目**：app/ · lib/ · components/ · proxy.ts · .qoder/ · specs/ 都覆盖到。先读 AGENTS.md / README.md / .qoder/business-logic/ 拿到约束和上下文，再扫代码。

2. **识别核心模块与相互关系**：
   - 考生端（开考 / 答题 / 提交 / 看成绩）的数据流
   - 管理端（鉴权 / 列表 / 详情 / 导出 / 重评）的能力清单
   - 评分 pipeline（客观 / 主观 / mock / 重试）
   - 存储层（内存 Map + globalThis 单例）

3. **输出到 ./wiki/ 目录**，分文件：
   - `01-项目概览.md` · 一句话定位 + 功能矩阵 + 待补功能
   - `02-架构与目录.md` · 各层职责 + 关键文件
   - `03-数据模型.md` · types.ts 中的 ExamRecord / Grading / Question 类型 + 题型存储约定
   - `04-评分管道.md` · 状态机（pending → grading → completed） + mock vs LLM
   - `05-鉴权机制.md` · proxy.ts + HMAC cookie
   - `06-API 清单.md` · 考生端 / 管理端 API 一览（含返回结构）

4. **每个 .md 都要**：
   - 头部有一句话总结
   - 引用具体文件:行号（`lib/grading.ts:438` 这种）
   - 列出"修改这个模块时容易踩的坑"
   - 不要复制大段代码（保留指针）

5. 完成后给我列一个文件清单 + 每个文件的"建议在什么 task 中先读"。
```

## 期望产出

`./wiki/` 目录下 6 份 .md，每份 30-80 行。后面 3 个 Task 开始前你都会查这里。

---

# 阶段 3 · Task 1 · 答卷详情页（15 分钟）

dashboard 上每个学员姓名是 link，点击会 **404** —— 这是 Task 1 的入口提示。

## 3.1 `/think` 启动（3 分钟）

```md
/think

我要做 Task 1 · 答卷详情页（详见 AGENTS.md §四）。请先：

1. 读 `.qoder/business-logic/question-types.md` —— 不同题型答案存储约定（特别是 multiChoice 的字母序列、scenario 的 JSON 字符串）
2. 读 `wiki/03-数据模型.md`（如已生成）
3. 给我输出：
   - Goal（一句话）
   - Non-Goals 至少 3 条
   - 影响范围：要新建 / 修改哪些文件？要不要新 API？
   - 候选方案 2 个：用 server component 直读 kv vs 新增 API + client fetch
   - 推荐选择 + 理由
   - 验证标准：什么算"做完"
   - 风险与回滚预案

**不允许直接开始写代码**，先 plan 让我确认。
```

## 3.2 `/challenge` 挑刺（2 分钟）

```md
/challenge

刚才的 plan 你扮演反对方挑至少 5 条刺，按 致命 / 严重 / 次要 分级，每条配具体场景。重点关注：

- 未作答的客观题 / 简答题怎么展示？会不会因为 `exam.answers[qid]` 是 undefined 而崩？
- 多选题的字母序列 "A,C,D" 在 UI 上怎么展示给人看？
- 简答题的 AI 评语含 mock 模式的 `[mock]` 前缀，要不要特殊处理？
- 鉴权层有没有什么我会误以为要重做但其实 proxy.ts 已经做了的事？
```

## 3.3 开 spec（2 分钟）

```bash
cp -r specs/_template specs/2026-05-12-task-1-exam-detail/
```

让 Agent 把 `/think` + `/challenge` 的结论填进去：

```md
请把刚才 /think 和 /challenge 的产出整理成 spec，写入：

- specs/2026-05-12-task-1-exam-detail/proposal.md（Goal / Non-Goals / 风险）
- specs/2026-05-12-task-1-exam-detail/design.md（架构 / API / 状态 / Rollback）
- specs/2026-05-12-task-1-exam-detail/tasks.md（拆 3-5 个 30 分钟以内的 checkpoint）
- specs/2026-05-12-task-1-exam-detail/progress.md（初始 "Status: 开始 T1"）

填完后 commit 这个 spec 目录，commit message: "spec: scaffold Task 1 exam-detail"
```

## 3.4 实施（6 分钟）

```md
/small-diff

按 tasks.md 一个一个 task 做。第一个 task 完成后**停下让我看 diff**，不要一口气全做完。
每个 task 写完 commit 一次。
```

实施过程中如果遇到问题：

```md
/hunt

[贴上报错或现象]

按 hunt 流程：复现 → 定位最近变更 → 提至少 2 个假设 → 逐个验证 → 找到 root cause 再动代码。未确认根因前**不允许**修改。
```

## 3.5 `/check` 收尾（2 分钟）

```md
/check

T1 我认为做完了，请按 check 流程审视：

- git diff 看一遍：是否超 scope？有没有顺手改无关代码？
- 对照 specs/2026-05-12-task-1-exam-detail/proposal.md 的 Goal 和 Non-Goals
- 跑 `pnpm typecheck` 和 `pnpm lint`
- 手测：访问 http://localhost:3001/admin/exam/seed_zhang_gaofen，应该能正常显示
- 输出审查报告
```

## 3.6 `/summary` 写回 progress（1 分钟）

```md
/summary

session 结束，按 summary 格式输出：

- 做完了什么（具体到 commit + 文件）
- 没做完什么
- 关键决策
- 下次从哪继续

把"progress.md 版"覆盖到 specs/2026-05-12-task-1-exam-detail/progress.md，然后 commit。
```

---

# 阶段 4 · Task 2 · 重评失败题（15 分钟）

dashboard 右上有 disabled 的"重评失败题"按钮，把它接通。

## 4.1 `/think`（3 分钟）

```md
/think

我要做 Task 2 · 重评失败题（详见 AGENTS.md §四）。请先：

1. 读 `.qoder/business-logic/grading-pipeline.md`，理解：
   - 状态机：pending → grading → completed
   - 失败题识别口径：**只**用 `feedback?.includes("评分失败")`，**不要**用 `score === 0`
   - `regradeFailedQuestions(exam)` 函数签名和返回结构（lib/grading.ts:438 附近）
2. 读 components/ClearDataButton.tsx，作为客户端组件的参考模板
3. 输出 Goal / Non-Goals / 影响范围 / API 契约 / 验证标准

⚠️ 提醒：`regradeFailedQuestions` **已经实现**，你的工作只是**写一个薄薄的 route handler 调用它** + 一个 client 按钮。**不要重写评分逻辑**。
```

## 4.2 `/challenge`（2 分钟）

```md
/challenge

刚才 plan 挑刺。重点：

- mock 模式下 feedback **不会**含"评分失败"，怎么演示 Task 2？
- 同一个 exam 并发被重评 N 次会怎样？要不要做 idempotency？
- 用户点完按钮失败时，UI 怎么提示？
- 返回结构 { attempted, recovered, stillFailing, scoreDelta } 哪些字段要展示给用户？
```

## 4.3 开 spec + 实施 + check + summary

按 Task 1 的同样 5 步流程：

```bash
cp -r specs/_template specs/2026-05-12-task-2-regrade/
```

用同样的 prompt 模板，把"task-1-exam-detail" 换成"task-2-regrade"。

## 4.4 演示 mock 下没有可重评目标怎么办

mock 模式没有"评分失败"——验证时可以**临时改 seed.ts** 注入一条带 `feedback: "评分失败"` 的记录：

```md
为了测 Task 2，临时在 lib/seed.ts 的张高分答卷的 grading.scores 里加一条
{ sa1: { score: 0, maxScore: 5, feedback: "评分失败，请联系管理员" } }，
然后重启 dev server。验证完成后再恢复。
**这个改动不要 commit。**
```

---

# 阶段 5 · Task 3 · 题型正确率统计（10 分钟）

侧边栏的"📈 题型正确率"链接指向 `/admin/stats`，目前 **404**。

## 5.1 `/think`（2 分钟）

```md
/think

Task 3 · 题型正确率统计页。请先：

1. 读 wiki/03-数据模型.md 了解 ExamRecord.grading.scores 和 breakdown 字段
2. 读 wiki/04-评分管道.md 理解 breakdown 各字段的 max 值（注意 historical 不一致：seed 用 39，gradeExam 旧 hardcode 26）
3. 输出 Goal / Non-Goals
   ⚠️ Non-Goals 必须包含："不引入图表库 / 不重算 grading 逻辑"
4. 候选方案：纯 server component 直接聚合 vs 新增 API + client 渲染
```

## 5.2 跨角色 review（关键）

```md
请扮演 @fe-architect 看一下方案，从前端架构视角挑 3 条问题。
然后扮演 @be-architect 看一下，从数据聚合视角挑 3 条问题。
两个角色的 review 不要互相覆盖——FE 关心组件 / 性能 / a11y，BE 关心聚合逻辑 / 数据契约 / 并发。
```

## 5.3 开 spec + 实施 + check + summary

同 Task 1 / 2 节奏，目录名 `specs/2026-05-12-task-3-stats/`。

实施时**强约束**：

```md
/small-diff

实施 Task 3。**禁止**：

- 引入 recharts / echarts / chart.js 等图表库
- 重新实现 grading 聚合逻辑（直接读 exam.grading.scores / breakdown）
- 把状态分散到多个组件（聚合一次性算完再传给 UI）

用纯 CSS：表格 + `<div style={{width:'XX%'}}>` 进度条。
```

---

# 阶段 6 · 沉淀 + 分享（5 分钟）

## 6.1 沉淀 1 条 gotcha

整个实验过程中你或 Agent 一定踩了至少 1 个坑。把它沉淀下来：

```md
/record-gotcha

整个 workshop 跑下来，挑 **1 个最值得沉淀的踩坑**，按 4 段格式：

- 现象
- 根因
- 正确做法（含代码位置引用）
- 反例（曾经试过但不行的做法）

写入 `.qoder/business-logic/gotchas.md`（不存在就建），commit。
```

## 6.2 最终 `/summary`

```md
/summary

整个 workshop 的总结，按 3 个版本输出：

1. **commit body 版**（给 git log 用）
2. **个人收获版**（给你自己回看用）
3. **一句话感悟版**（30 秒内能讲完，分享用）

重点突出：
- 真的体验到了哪个 Skill / 方法的价值
- 最意外的发现
- 回去想立刻在自己项目里用的 1 件事
```

## 6.3 commit + push

```bash
git add specs/ wiki/ .qoder/business-logic/gotchas.md app/ components/
git commit -m "workshop: 完成 3 个 Task + 沉淀 gotcha + spec 全套"
git push
```

---

# 加分项 · llm-wiki 实战（课后探索 / 时间够再做）

把刚才生成的 `wiki/` 接入一个**真正的 Wiki Agent**，体验"团队记忆 + 主动维护"是什么感觉。

## 安装

```bash
# 全局安装
npm install -g @jackwener/llm-wiki

# 初始化一个新的 wiki vault
cd ~
mkdir my-wiki && cd my-wiki
llm-wiki init

# llm-wiki 默认建 .claude/，Qoder 用户改名为 .qoder/
mv .claude/ .qoder/
```

## 验证身份

```md
你好，你的角色是什么？
```

期望 Agent 回答它是 llm-wiki maintainer / 记录者，负责 ingest / compile / lint / query。

## 摄入 exam-system wiki

```md
/llm-wiki

你现在是 exam-system 项目的 LLM Wiki 记录者，以后这是你的工作职责。

目前 exam-system 已经有初始的 wiki 文档：
/Users/jakevin/Documents/vivo/exam-system/wiki/

请对它们进行 ingest 摄入：
1. 逐份读完
2. 为每篇生成 metadata（source / last_confirmed / confidence）
3. 抽取关键实体（模块 / 接口 / 决策 / 踩坑）建立 graph
4. 摄入完毕后，列出 wiki 的 "目录索引"（项目页 / 模块页 / 接口页 / gotcha 页 各几个）
```

## 用 wiki 回答问题

摄入完成后试着问：

```md
/query 评分失败的题怎么识别？识别后怎么重跑？引用具体代码位置。
```

```md
/query 管理员鉴权用什么机制？为什么不直接用 NextAuth？
```

```md
/lint
（让 wiki agent 自检过期 / 孤立 / 矛盾 / 断链）
```

## 反哺：把今天踩的坑也 ingest 进去

```md
/llm-wiki

把 /Users/jakevin/Documents/vivo/exam-system/.qoder/business-logic/gotchas.md
ingest 进来，标 source 为 "workshop-2026-05-12"。
这样未来 query 类似问题时能命中今天的发现。
```

---

# 常见问题

## Q1 · `pnpm dev` 起不来 / 端口被占

```bash
# 看占用端口的进程
lsof -ti :3001
# 杀掉
kill $(lsof -ti :3001)
# 或换端口启动
PORT=3002 pnpm dev
```

## Q2 · 登录提示"密码错误"，但密码确实是 workshop2026

1. 看 `.env.local` 是否覆盖了 `ADMIN_PASSWORD`（应该不存在或留空）
2. 输入框可能 autofill 了别的密码 → 用无痕窗口
3. 直接 curl 验证：

```bash
curl -i -X POST http://localhost:3001/api/admin/auth \
  -H "Content-Type: application/json" \
  -d '{"password":"workshop2026"}'
```

返回 200 + Set-Cookie 就是对的，问题在浏览器。

## Q3 · `git commit` 被 pre-commit hook 拒绝

**这是设计行为**——hook 会跑 typecheck + lint + reward-hack 检查。错误信息里会有 `🤖 GUIDANCE FOR THE AI AGENT` 段，**读它，让 Agent 修问题**，不要：

- ❌ `git commit --no-verify`
- ❌ 加 `// @ts-ignore` 没 `reason:` 注释
- ❌ `.skip()` / `.only()` 测试用例

## Q4 · mock grader 没有"评分失败"，Task 2 怎么验证？

见 §4.4：临时改 `seed.ts` 注入一条带 `feedback: "评分失败"` 的记录，验证完恢复（不 commit）。

## Q5 · 三个 Task 都做完了还有时间？

按优先级做：

1. 在 Task 1 详情页加"未作答客观题用红色叹号高亮"
2. 在 Task 2 重评按钮上加 confirm（"确认重评？此操作会消耗 LLM 配额"）
3. 在 Task 3 加按"日期 / 班级"筛选答卷
4. 开始玩 llm-wiki 部分（加分项）

---

# 实验完成 checklist

回去之前确认：

- [ ] 3 个 Task 都至少做了 spec（即便没全部实现）
- [ ] 至少**完整跑通**了 1 个 Task 的全流程：`/think` → `/spec` → `/small-diff` → `/check` → `/summary`
- [ ] 用过 `/challenge` 至少 1 次，能感觉到"自己挑自己刺"的价值
- [ ] 用 `/record-gotcha` 沉淀了至少 1 条具体 gotcha
- [ ] `.qoder/business-logic/gotchas.md` 在 git 里有 commit
- [ ] 能用 1 句话答："今天最意外的发现是 ___"

不追求完美。追求**走完了一次完整的方法论闭环**。

---

> 培训理念："Spec + Workflow + Validation + State Management"——
> 让 Agent 从"不稳定的天才"变成"在明确边界里高效执行的工程伙伴"。
>
> 今天的练习就是把这套方法论在自己手指头上跑一遍。下周回到自己项目，
> 你应该能本能地说："等等，先 /think 一下；这个不太对，让我 /check 一下。"
