# 实验手册

## ⚠️ 前置认知：两个目录、两个 Qoder 窗口

整个实验涉及**两个独立项目**，**分别用 Qoder 打开两个窗口**——不要在同一个窗口里切。

| 窗口 | 目录 | 角色 |
|---|---|---|
| **A · exam-system** | `~/work/exam-system/`（任意路径） | 做项目开发：3 个 Task |
| **B · llm-wiki** | `~/my-wiki/` | 团队长期记忆库：摄入 wiki / summary，对外查询 |

**两个窗口之间通过文件传递**——A 产生的 `wiki/*.md` 和 `/summary` 输出，复制到 B 让 llm-wiki ingest。这种"一前一后、交替推进"才是真正的"开发 → 沉淀 → 复利"闭环。

---

## Step 1 · 启动 exam-system（窗口 A）

```bash
git clone -b workshop git@github.com:jackwener/exam-system.git
cd exam-system && pnpm install && pnpm dev
# http://localhost:3001/admin/login · 密码 workshop2026
```

登录后 dashboard 应该有 3 份 seed 答卷。

## Step 2 · 在 exam-system 里生成项目初始 wiki（窗口 A）

```md
我是一名研发工程师，计划基于当前的 exam-system 项目开发新功能。请帮我：

1. 扫描并分析整个项目目录结构（含 app/ · lib/ · components/ · proxy.ts · .qoder/）
2. 识别核心模块及其相互关系：考生端数据流、管理端能力、评分 pipeline、存储层、鉴权
3. 分析架构与隐性约束（先读 AGENTS.md 和 .qoder/business-logic/ 拿到全局上下文）
4. 整理 API 接口、题型存储约定、评分状态机
5. 输出成若干份 Markdown 放到 ./wiki/，分文件：项目概览 / 架构 / 数据模型 / 评分管道 / 鉴权 / API 清单

每篇带 file:line 引用、列"修改时容易踩的坑"，不复制大段代码。
```

完成后 `./wiki/` 应该有 6 份 .md。

## Step 3 · 启动 llm-wiki + 摄入项目 wiki（窗口 B）

打开一个**新 Qoder 窗口**指向 llm-wiki 目录：

```bash
npm install -g @jackwener/llm-wiki

cd ~ && mkdir my-wiki && cd my-wiki
llm-wiki init
mv .claude/ .qoder/   # llm-wiki 默认建 .claude/，Qoder 用户改名
```

用 Qoder 打开 `~/my-wiki`，验证身份：

```md
你好，你的角色是什么？
```

期望 Agent 回答自己是 llm-wiki maintainer。然后摄入 exam-system 的 wiki：

```md
/llm-wiki

你现在是 exam-system 项目的 wiki maintainer，这是你的长期职责。

请 ingest 这些初始文档：
/Users/jakevin/Documents/vivo/exam-system/wiki/

- 逐份摄入，抽取 metadata（source / last_confirmed / confidence）
- 建立实体图谱（模块 / 接口 / 决策 / 踩坑）
- 摄入完毕后列出"目录索引"
```

✅ 现在 llm-wiki 已经"上岗"。后面每完成一个 Task，都回这个窗口喂 `/summary`。

---

## Step 4 · Task 1 · 答卷详情页（窗口 A）

```md
/think

做 Task 1 · 答卷详情页（详见 AGENTS.md §四）。
先读 .qoder/business-logic/question-types.md（题型存储约定）。
输出 Goal / Non-Goals / 影响范围 / 候选方案 / 验证标准，**不允许直接写代码**。

plan 完用 /spec 落 5 文件 spec 到 specs/2026-05-12-task-1/，
实施期间用 /small-diff，完成用 /check 收尾，最后 /summary。
```

### 4.1 Task 1 完成后，输出 /summary

```md
/summary

把本次 session 的所有内容凝结成可被 llm-wiki ingest 的 session digest：

- 做了什么（具体到 commits / 文件 / 关键变更）
- 关键决策（为什么选 A 不选 B）
- 踩坑与 root cause
- 涉及 / 更新了哪些模块
- 还没做完的部分 + 下次从哪继续

格式：清晰分段、有具体引用、不要把调试折腾过程当 summary。
输出后 commit 到 spec 的 progress.md。
```

### 4.2 切到窗口 B，把 summary 喂给 llm-wiki

```md
/ingest

刚完成 exam-system Task 1，下面是这个 session 的 digest：

[粘贴 Step 4.1 的 /summary 输出]

请 ingest：
- 关联到模块 admin-exam-detail
- 标 source: workshop-2026-05-12 · task-1
- 抽取"关键决策"和"踩坑"分别建立 decision / gotcha 节点
- 更新涉及的模块页（如果有内容补充）
```

---

## Step 5 · Task 2 · 重评失败题（窗口 A）

```md
/think

做 Task 2 · 重评失败题。
先读 .qoder/business-logic/grading-pipeline.md（重点：失败题用 feedback?.includes("评分失败") 识别）。
lib/grading.ts 已有 regradeFailedQuestions 辅助函数，**只需写薄薄的 route handler 调用它** + 把 dashboard 上 disabled 的按钮替换成 client 组件（参考 components/ClearDataButton.tsx）。

plan 完用 /spec，实施用 /small-diff，完成用 /check + /summary。
```

> mock 模式下 feedback 不会含"评分失败"——验证时临时改 `lib/seed.ts` 给张高分注入一条 `feedback: "评分失败"` 的记录（验完恢复，不 commit）。

### 完成后 → 同样的 /summary → 切到 B 窗口 → /ingest（标 task-2）

---

## Step 6 · Task 3 · 题型正确率统计（窗口 A）

```md
/think

做 Task 3 · 题型正确率统计页。
聚合所有 completed 答卷，输出每个题型平均得分百分比 + 每道客观题正确率。
**禁止**：引图表库（用纯 CSS `<div style={{width:'78%'}}>` 进度条）/ 重写 grading 逻辑。

请同时扮演 @fe-architect 和 @be-architect 各挑 3 条刺再实施。
```

### 完成后 → 同样的 /summary → 切到 B 窗口 → /ingest（标 task-3）

---

## Step 7 · 在 llm-wiki 验证闭环（窗口 B）

3 个 Task 都喂完，wiki 里会有：

- 6 篇初始模块文档（来自 Step 3）
- 3 条 session digest（来自 Step 4 / 5 / 6）
- 若干 decision / gotcha 节点

试着查询：

```md
/query 评分失败的题怎么识别？引用具体代码位置 + 今天踩到的坑。
```

```md
/query 管理员鉴权用什么机制？为什么不用 NextAuth？
```

```md
/lint
```

✅ 如果 wiki 能答出今天工作里的细节，**复利闭环就跑通了**——这就是培训讲的"开发 → summary → wiki → 下一次开发的输入"。

---

# 进阶任务（可选）· 配置 Qoder Action

让 Qoder 在 GitHub 上自动 review PR + 响应 `@qoder` mention。文档：https://docs.qoder.com/zh/cli/qoder-action

> 这是**可选**任务。前 7 步搞定后有时间再做。

## A1 · 本地准备 Qoder CLI

```bash
# 安装 Qoder CLI（如果还没装）
# 见 https://docs.qoder.com/zh/cli/installation

# 登录
qoder login

# 进入交互式：在 qoder CLI prompt 里输
/model
```

`/model` 应该列出可用模型 + **当前 credit 配额**。**没配额 = action 跑不通**，必须先在 qoder.com 看账户状态。

## A2 · 把 GitHub 绑到 Qoder

在 qoder CLI 里输：

```
/setup-github
```

按引导走完：配置好 GitHub App 连接，生成一个 token 并保存。

## A3 · 验证 workflow 已就位

本仓库**已经合并**了 2 个 workflow（PR #1）：

- `.github/workflows/qoder-auto-review.yml` —— PR 触发自动 review
- `.github/workflows/qoder-assistant.yml` —— `@qoder` mention 触发交互

## A4 · 验证

随便提个 PR，等几分钟看 Actions 标签是否绿。如果失败：

| 报错 | 原因 |
|---|---|
| `no valid model config found` | token 无效 / 账户没绑 GitHub / 没装 qoderai App / 配额耗尽 |
| `authentication failed` | secret 名字拼错 / token 过期 |

修复后 PR 页面 → **Re-run failed jobs**。

试着在 issue 或 PR 评论里 `@qoder explain xxx` 看响应。

---

## 完成判据

- [ ] 3 个 Task 至少 1 个走完完整闭环：`/think → /spec → 实施 → /check → /summary → /ingest`
- [ ] llm-wiki 里有摄入的项目 wiki（6 篇）+ 至少 1 条 session digest
- [ ] llm-wiki `/query` 能命中今天的工作内容
- [ ] （可选）Qoder Action 在新 PR 上跑绿
- [ ] 一句话讲清楚："今天体验到 ___ 是最有价值的"
